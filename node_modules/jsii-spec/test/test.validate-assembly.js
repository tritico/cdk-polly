"use strict";
const nodeunit_1 = require("nodeunit");
const validate_assembly_1 = require("../lib/validate-assembly");
module.exports = nodeunit_1.testCase({
    validateAssembly: {
        'rejects invalid assembly'(test) {
            test.throws(() => validate_assembly_1.validateAssembly({}));
            test.done();
        }
    }
});
